# AGENTS.md — 幽兰工程记忆维护协议

本文件规定 Agent 在本仓库中的启动、记忆同步和任务收尾方式。项目长期记忆位于 `wiki_memory/`。

## 会话开始时的读取顺序

1. 读取本文件。
2. 读取 `wiki_memory/当前状态/项目概览.md`、`系统架构.md`、`当前约束.md`、`当前待办.md`。
3. 按当前任务读取相关 active 决策和知识页。
4. 只有需要追溯时才读取最近 1–3 篇工作日志。

原始代码、配置、模型和用户资料是事实来源；记忆只记录结论、关系和相对路径，不复制大段原文或命令输出。

## 记忆模型

- `wiki_memory/当前状态/`：当前有效的项目事实，一个主题只保留一个 active 版本。
- `wiki_memory/决策/`：已确认的架构和技术决策；被替代的页面保留并标记状态。
- `wiki_memory/知识/`：稳定的模块、流程、规范和运维知识。
- `wiki_memory/日志/`：每次实质任务的追加式历史，不作为当前事实的唯一来源。

## 记忆同步

- 完成实质任务后新增 `wiki_memory/日志/YYYY-MM-DD-任务标题.md`，记录目标、决策、范围、文件、验证、结果和遗留问题。
- 日志使用 YAML frontmatter；路径和 Obsidian 链接统一使用 `/`。
- 新增或修改日志后运行 `python wiki_memory/工具/memory_lint.py index`，再运行 `python wiki_memory/工具/memory_lint.py check`。
- 只有用户确认或用户直接指定的长期事实才能进入 active 状态页、决策页或知识页。
- 不在记忆中记录密钥、令牌、私人消息、内部推理或大段终端输出。

## 俗成规定：完整任务提交并推送

- “修改完整的一次任务”指一个已经实现、完成必要验证、同步工程记忆并可独立验收的交付单元。
- 每完成这样的一个任务，就创建一个语义明确的 Git 提交，并将当前分支推送到 `origin`；原则上一个完整任务对应一个提交。
- 提交前先同步远端并处理非快进状态，不覆盖远端成果，不把无关的用户改动混入提交。
- 若远端不可用、鉴权失败、分支受保护或用户明确要求不提交，必须保留本地成果并如实说明阻塞原因。

## 固定操作

- `记忆同步`：新增工作日志、更新必要的已确认长期记忆并刷新索引。
- `记忆检索 <关键词>`：先查当前状态和日志 MOC，再查相关知识与历史。
- `记忆体检`：运行记忆 lint，报告断链、缺字段、重复 active 主题和孤儿页面。
- `记忆压缩`：把重复历史归纳到长期页面，不删除原始日志。
