---
type: log
status: archived
kind: bug
importance: high
updated: 2026-09-03
topic: viewer-controls-and-project-memory
source_logs: []
supersedes: null
---

# 2026-09-03｜构建工程记忆并修正视角控制

- 时间：2026-09-03（北京时间）
- 类型：`bug`
- 状态：完成
- 目标：建立 wiki_memory、固化 Git 收尾约定，并修复 OrbitControls 轴向体感。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]

## 已确认的决策

- 用户确认采用 `工程记忆构建` 中 `wiki_memory` 的分层、日志、MOC 和 lint 思路。
- 用户确认俗成规定：“修改完整的一次任务，就提交并推送”。
- 模型通过根节点旋转适配 Three.js 标准 Y-up / Z 视线坐标，不再改变相机 up 轴迁就模型。

## 检查与操作

- 检查项目结构、Git 分支与远端差异、Blender 导出资产和 transmission 原始示例。
- 对比当前 `frameModel()` 与原示例的相机位置、默认 up 轴和 OrbitControls 初始化。
- 建立当前状态、决策、知识、日志和 lint 工具目录。

## 文件变更

- 新增根目录 `AGENTS.md` 与 `wiki_memory/`。
- 修改 `src/viewer/createSpecimenViewer.js` 的模型朝向、相机轴系和自适应取景。
- 本次提交同时纳入此前已完成但尚未提交的双模型 Transmission 查看器、GLB、Blender 导出脚本和本地示例依赖。

## 测试与验证

- 记忆索引已刷新，lint 检查 19 个 Markdown 页面并通过。
- `npm run build` 通过，模型、HDR 与应用资源均成功打包；仅保留 Three.js 单包体积提示。
- 真实 Chrome 中确认仅有 1 个 Canvas 和 1 个 GUI，中文分组完整，控制台、页面和网络请求无错误。
- 分别模拟水平与垂直鼠标拖动：水平拖动显示模型侧边，垂直拖动显示上下表面，交互方向符合原 transmission 示例。
- Git 提交与远端推送结果在任务最终回复中报告。

## 待确认长期记忆

- 无；本次用户已直接确认项目记忆结构和 Git 收尾约定，相关内容已写入 active 页面。

## 问题、结果与下一步

- 结果：wiki_memory、提交推送规范和视角控制修复均已完成验证。
- 遗留问题：暂无。
- 下一步：按用户后续任务继续维护。
