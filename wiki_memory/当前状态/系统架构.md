---
type: state
status: active
kind: architecture
importance: high
updated: 2026-09-05
topic: system-architecture
source_logs:
  - "[[日志/2026-09-05-新增纸飞机环游星球场景]]"
  - "[[日志/2026-09-05-按参考工程重构逐瓣绽放与脱落飘散]]"
  - "[[日志/2026-09-04-实现隔离杜鹃花无限绽放场景]]"
  - "[[日志/2026-09-04-分析参考视频并实现隔离金菊闪柳场景]]"
  - "[[日志/2026-09-04-实现隔离花粉星云场景]]"
  - "[[日志/2026-09-04-深度修复指针视差与轨道交接]]"
  - "[[日志/2026-09-04-分析参考视频并实现指针相机视差]]"
  - "[[日志/2026-09-04-为太阳光束增加边缘与时间渐隐]]"
  - "[[日志/2026-09-04-修正太阳出画后才隐藏光束]]"
  - "[[日志/2026-09-04-统一纵深面板并增加无限远太阳]]"
  - "[[日志/2026-09-03-分析双视频并实现尽头迎光与动态混色]]"
  - "[[日志/2026-09-03-修正连续外壳并交付深邃默认效果]]"
  - "[[日志/2026-09-03-替换透明模型并增加原生自发光]]"
  - "[[日志/2026-09-03-实现切片颜色累积与HDRI分级显色]]"
  - "[[日志/2026-09-03-增加HDRI设置与基础阵列]]"
  - "[[日志/2026-09-03-构建工程记忆并修正视角控制]]"
  - "[[日志/2026-09-03-替换单网格双材质槽模型]]"
supersedes: null
---

# 系统架构

## 架构概览

这是一个无后端的 Vite 单页应用。React 只负责挂载和卸载查看器；Three.js 负责 GLB/HDR 加载、物理透射材质、相机、OrbitControls、五个隔离 Scene 和 WebGL 渲染；lil-gui 直接修改活动场景的专属参数与共享资源参数。

## 模块关系

| 模块 | 职责 | 依赖 | 入口 |
| --- | --- | --- | --- |
| React 壳层 | 挂载查看器并显示中文加载错误 | React | `src/App.jsx` |
| 三维查看器 | 共享渲染器、相机、控制器、GUI 与五场景生命周期 | Three.js 本地核心与 addons | `src/viewer/createSpecimenViewer.js` |
| 场景隔离切换 | 活动 Scene、相机状态与场景专属面板显隐 | PerspectiveCamera、OrbitControls、lil-gui | `src/viewer/sceneSwitcher.js` |
| 花粉星云 | 三层批量粒子、GPU 流场、中央核心与参数面板 | Points、BufferGeometry、ShaderMaterial | `src/viewer/pollenScene.js` |
| 金菊闪柳烟花 | 升空、金菊主枝、枝端闪烁、闪柳轨迹、独立夜空与时间线 | Points、LineSegments、BufferGeometry、ShaderMaterial | `src/viewer/fireworkScene.js` |
| 烟花后处理 | 场景3线性原画、分辨率缩放 Bloom 与最终输出 | UnrealBloomPass、OutputPass、WebGLRenderTarget | `src/viewer/fireworkPost.js` |
| 无限花开 | 独立花瓣年龄、持续展开、外瓣脱落飞行与真实花瓣材质 | InstancedMesh、MeshPhysicalMaterial、ShaderMaterial | `src/viewer/infiniteBloomScene.js`、`bloomPetals.js` |
| 纸飞机环游 | 用户纸飞机与地球资产、立体环绕航道与独立场景 | InstancedMesh、GLTFLoader、WebGL | `src/viewer/paperOrbitScene.js`、`paperOrbitMotion.js` |
| 环境管理 | 白色环境、图片解码替换、强度与资源释放 | 本地 UltraHDR 与 addons HDR/EXR | `src/viewer/environmentManager.js` |
| 纵深几何 | 1–200 层唯一状态、同帧生成及远裁剪保护 | 复用 arrayModifier 基础双分组生成 | `src/viewer/depthStack.js` |
| 扩展面板 | HDRI/累积控件与状态反馈、用途分组及说明折叠 | 原生 lil-gui | `src/viewer/viewerPanels.js`、`panelOrganization.js` |
| 深邃呈现 | 默认调校、纵深预设、首层取景与物理对照 | 原生 lil-gui、Three.js | `src/viewer/depthPresentation.js` |
| 局部发光 | 独立 UV1 和原生 emissiveMap 光纹 | CanvasTexture | `src/viewer/localEmission.js` |
| 选择性光晕 | 线性原画、局部发光代理、Bloom 与最终输出 | UnrealBloomPass、OutputPass | `src/viewer/selectiveBloom.js` |
| 梦境环境 | 无限远/有限距离亮心、程序混色背景、迎光与遮挡近似 | ShaderMaterial、现有覆盖纹理 | `src/viewer/dreamAtmosphere.js` |
| 指针视差 | 指针映射、临时视平面偏移、可见姿态提交、轨道交互状态机与缓动 | Three.js Camera、OrbitControls、Pointer Events | `src/viewer/pointerParallax.js` |
| 柔透绘制 | 视频参考透明呈现的分组内三角形排序 | 保留单标本 Mesh、双 groups | `src/viewer/transparentOrdering.js` |
| 模型资产 | 保存一个具名网格及外框、内框两个材质区域 | GLB | `public/models/specimen-frame.glb` |
| 模型接入 | 合并加载器产生的两个材质原语为单网格双分组 | Three.js BufferGeometryUtils | `src/viewer/specimenModel.js` |
| Blender 导出 | 选择指定对象并生成 GLB | Blender Python API | `scripts/export_specimen.py` |
| 杜鹃花导出 | 连通区域拆分、Closed Morph 与 2K 纹理运行时资产 | Blender Python API | `scripts/export_azalea_bloom.py` |
| 构建配置 | 将 bare `three` 精确映射到本地 r186dev 核心 | Vite | `vite.config.js` |

## 数据流

源文件 `source/Specimen_Frame_Transparent.blend` 的对象 `SPECIMEN_OUTER_FRAME` 仅在后台导出内存中规范为运行时名称 `SPECIMEN_FRAME_MATERIAL_SLOTS`，不保存源文件。此前内部侧壁诊断与导出修正见关联工作日志。自发光使用原生 `emissive` / `emissiveIntensity` / `emissiveMap`；后处理扩散独立提取的局部发光与尽头亮心，径向效果在线性阶段合成，再统一色调映射。

Blender 源文件经导出脚本生成单网格、双 primitive 的 GLB；浏览器独立加载模型，将两个原语按材质名称合并为单 Mesh、两个 groups，分别绑定独立 `MeshPhysicalMaterial`，仅颜色读取源材质。环境照明在白色兜底上独立加载指定内置 EXR，可见背景默认使用程序混色；模型就绪后应用调好的默认纵深、相机和局部发光。纵深从不可变基础几何生成，保留两个材质分组及 UV1。静态时 GUI 变更和相机操作触发按需重绘，背景流动启用且页面可见时持续绘制，同帧纵深修改只生成一次几何。

场景选择器在标本、花粉、烟花、无限花开和纸飞机环游五个 Scene 之间切换活动渲染目标，分别保存相机与 OrbitControls 状态。场景1/2共用同一环境纹理和程序背景时间/配色；花粉 Scene 使用独立背景材质，不读取标本覆盖计数。场景3共享 HDRI 资源状态但其自定义粒子不读取物理环境照明，并使用独立夜空时间/配色和独立 Bloom。只有活动 Scene 执行其更新与绘制管线。

场景4共享 HDRI 资源并保留独立背景。用户最新要求用逐瓣生命周期替代完整花冠叠加：新瓣持续从内向外展开，老瓣保持脱落姿态独立飘散，飞行不随花开周期重置。具体批次与着色器实现见 [[日志/2026-09-05-按参考工程重构逐瓣绽放与脱落飘散]]。其资源与相机状态仍不进入场景1–3。

用户指定新增场景5，使用真实纸飞机与地球模型围绕星球飞行。该场景的航道、模型导出与本轮验证见 [[日志/2026-09-05-新增纸飞机环游星球场景]]；具体默认美术参数尚未确认为长期规范。

## 运行与部署

用户最新四项视频参考目标与材质调校取代静态可见天空默认，见 [[决策/ADR-007-视频参考梦境环境与柔透呈现|ADR-007]]。独立背景进入透射渲染，环境照明仍独立加载；日轮发光与径向效果在线性阶段合成并统一输出。材质低覆盖与排序属于美术近似，物理对照保持可用。动态背景仅在可见且开启时持续绘制，关闭/暂停后仍按需刷新。

太阳投影在 CPU 侧计算圆盘屏幕半径和到最近视口边缘的有符号距离；`smootherstep` 生成空间软门限，帧率无关阻尼生成时间响应，统一驱动动态背景迎光、线性径向光与太阳 Bloom。过渡结束后渲染循环回到按需状态；光束扩散只改变屏幕衰减距离，不参与显隐。

空闲时，指针视差在每帧透明排序与梦境环境更新前临时移动相机，并在全部绘制结束后恢复基础位置和四元数；因此所有三维层和屏幕光效共享同一视角。OrbitControls 开始交互时，模块把当前可见姿态提交为新的基础相机与轨道状态，再冻结视差；结束时默认以最后指针位置重定相对输入原点，避免基础姿态与可见姿态之间跳变。视差缓动或梦境背景任一仍活动时继续请求下一帧，两者都静止后停止。

本地开发使用 `npm run dev`，生产检查使用 `npm run build`。产物是静态站点，不包含后端接口。

## 相关决策

用户后续要求研究实现切片颜色累积；新增显示扩展的实现记录见 [[日志/2026-09-03-实现切片颜色累积与HDRI分级显色|切片累积工作日志]]。该扩展不替换本地核心和双槽架构。

- [[决策/ADR-008-统一纵深控制与无限远亮心|统一纵深控制与无限远亮心]]
- [[决策/ADR-010-双场景隔离与共享背景|双场景隔离与共享背景]]
- [[决策/ADR-011-隔离烟花场景与GPU批量轨迹|隔离烟花场景与 GPU 批量轨迹]]
- [[决策/ADR-013-无限花开按花瓣年龄展开与脱落|按花瓣年龄展开与脱落]]
- [[决策/ADR-009-指针驱动三维相机视差|指针驱动三维相机视差]]
- [[决策/ADR-007-视频参考梦境环境与柔透呈现|视频参考梦境环境与柔透呈现]]
- [[决策/ADR-002-模型转为标准Three视角坐标|模型转为标准 Three 视角坐标]]
